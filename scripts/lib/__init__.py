# Shared helpers for the Sun Walk build scripts. Import path: `scripts.lib.*`
# (run scripts with the repo root as cwd) or `lib.*` (with `scripts/` on the
# path). Stdlib only, like every script under scripts/.
